#include <iostream>
using namespace std;
#include "file.h"
int main()
{
	//file();//P30


	//P50
	/*
		int* p = new int[10];
		delete[] p;
		p = NULL;
		delete[] p;//程序不会报错，编译器会帮我们检查p是否为空

		//习惯问题删除前还是要判断下p是否为空
		if(p)
		{
			delete[] p;
		}

		int * p = new int(0);
		delete p;
		p = NULL;//这句不能少
		delete p;
	*/



	return 0;
}